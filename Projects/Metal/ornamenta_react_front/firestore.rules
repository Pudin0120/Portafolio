rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ALLOW READ AND WRITE ON ANY DOCUMENT
    // IF THE USER IS AUTHENTICATED.
    // DO NOT USE IN PRODUCTION!
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
